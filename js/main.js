'use strict';

// Entry point — build dynamic UIs, wire all events, then do the initial render.
buildNumberLineUI();
buildGeometry3DUI();
buildBalanceUI();
buildPanBalanceUI();
buildVolumeMeasuresUI();
wireAll();
render();
